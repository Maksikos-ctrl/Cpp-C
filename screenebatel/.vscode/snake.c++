#include "stdafx.h"
#include <Windows.h>

int scrWidth
int scrHeight
int interval = 100;

LRESULT CALLBACK Melt(HWND hWnd, UNIT msg, WPARM wPAram, LPARAM lParam)
{
     switch (msg)
     {
     case WM_CReate:
     {
         break;
     }  
     case
     }
}